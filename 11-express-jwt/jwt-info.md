 JWT abbreviation stands for "JSON Web Tokens" and can be considered to be a form of user identification that is issued after the initial user authentification takes place. When a user completes their login process and is authenticated our RestAPI will issue the client application an access token and a refresh token. The access token is given a short time before it expires (for example, 5 to 15 minutes) and the refresh token is given a longer duration before it expires (possibly several hours to even days). While no security measures are perfect we do want to consider the risks of cross-site scripting and cross-site request forgery. 
 
Our API will send and receive access tokens as JSON data to avoid the previously mentioned risks it is recommended for frontend client applications to only store access tokens in memory, so they'll be automatically lost when the app is closed. They should not be stored in local storage or in a cookie. Essentially if you can store it with JavaScript a hacker can also retrieve it with JavaScript. Just keep access tokens in memory which you might also refer to as the current application state. 

Our API will issue refresh tokens in an HTTP-only cookie. This type of cookie is not accessible with JavaScript. Refresh tokens do need to have an expiration which will then require users to log in again. Refresh tokens should not have the ability to issue the new refresh tokens because that essentially grants indefinite access if a refresh token falls into the wrong hands. So the overall access token process involves issuing an access token during user authorization. 

The user's application can then access our RestAPI's protected routes with the access token until it expires. Our API will verify the access token with middleware everytime the access token is used to make a request. When the access token does expire the user's application will need to send their refresh token to our API's refresh endpoint to get a new access token. Of course, the refresh token is also issued during user authorization. Our RestAPI's refresh endpoint will verify the token and cross-reference the refresh token in our database too. Storing a reference to the refresh token in the database will allow refresh tokens to be terminated early if the user decides to log out. And again refresh tokens need to be allowed to expire, so indefinite access cannot be gained.

(Go to [11-express-jwt/server.js])

[//]: #First issue with "Frontend - fetch method and its 'credentials' option'": (https://youtu.be/f2EqECiTBL8?si=Q_ABAYStKWTZzFMA&t=18134)
[//]: #Third issue with "Cookies options 'sameSite' & 'secure'"  (https://youtu.be/f2EqECiTBL8?si=GT2Z5ovM4tSY4QaT&t=18353)